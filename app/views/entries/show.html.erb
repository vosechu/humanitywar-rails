<% @body_id = "entries" %>
<h2>Wins: <%= @entry.wins %></h2>
<div class="cf entries">
  <div class="black-card"><%= @entry.black_card %></div>
  <% @entry.white_cards.each_with_index do |card, index| %>
    <div class="white-card"><%= card %></div>
  <% end %>
</div>