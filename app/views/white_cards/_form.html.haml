= form_for @white_card do |f|
  - if @white_card.errors.any?
    #error_explanation
      %h2= "#{pluralize(@white_card.errors.count, "error")} prohibited this white_card from being saved:"
      %ul
        - @white_card.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :text
    = f.text_field :text
  .actions
    = f.submit 'Save'
